<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zcx.mapper.UserMapper">
	<select id="queryUserByNameAndPwd" resultType="com.zcx.pojo.User">
		select * from tbl_user where uname=#{param1} and pwd=#{param2}
	</select>
</mapper>